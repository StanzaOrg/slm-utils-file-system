
#use-added-syntax(tests)
defpackage file_system_utils/tests :
  import core

  import file_system_utils

deftest test-get-cwd:
  val cwd = get-cwd()
  ;println("cwd: \"%_\"" % [cwd])
  #EXPECT(length(cwd) > 0)
